<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Source generators must target netstandard2.0 -->
    <TargetFramework>netstandard2.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>

    <!-- Roslyn plumbing -->
    <IsRoslynComponent>true</IsRoslynComponent>
    <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>

    <!-- NuGet metadata -->
    <PackageId>Dragonfire.Logging.Generator</PackageId>
    <Version>1.0.0</Version>
    <Authors>Dragonfire</Authors>
    <Description>
      Roslyn source generator for Dragonfire.Logging.
      Generates compile-time logging proxies for services implementing ILoggable,
      replacing the runtime DispatchProxy with zero reflection overhead at execution time.
    </Description>
    <PackageTags>logging;source-generator;aop;proxy;structured-logging</PackageTags>
    <DevelopmentDependency>true</DevelopmentDependency>
  </PropertyGroup>

  <!-- When packed as NuGet, the generator DLL goes into analyzers/dotnet/cs
       so MSBuild auto-registers it as an analyzer in consuming projects. -->
  <ItemGroup>
    <None Include="$(OutputPath)\$(AssemblyName).dll" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
  </ItemGroup>

  <ItemGroup>
    <!-- Only needed at generator build time; never flows to consuming projects -->
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="4.8.0" PrivateAssets="all" />
    <PackageReference Include="Microsoft.CodeAnalysis.Analyzers" Version="3.3.4" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\..\README.md" Pack="true" PackagePath="\" Condition="Exists('..\..\README.md')" />
  </ItemGroup>


</Project>
